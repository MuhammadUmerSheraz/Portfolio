# Canonical URL for SEO (Open Graph, sitemap, JSON-LD). No trailing slash.
# Production: https://your-domain.vercel.app
# Optional on Vercel if unset — VERCEL_URL is used automatically.
# NEXT_PUBLIC_SITE_URL=https://example.com
